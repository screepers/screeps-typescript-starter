{
  // This is an example credentials configuration used by the
  // `screeps-webpack-plugin`. Ideally on the official server you'll only need
  // `email`, `password`, and `serverUrl`. Take what you need, and remove the rest.
  //
  // (P.S. Don't forget to remove this comment too. Some JSON parsers don't
  // recognise comments.)
  //
  // More configurations available:
  // https://github.com/langri-sha/screeps-webpack-plugin#usage
  "email": "",
  "password": "",
  "token": "",
  "serverUrl": "https://screeps.com",
  "serverPassword": "",
  "gzip": false
}
